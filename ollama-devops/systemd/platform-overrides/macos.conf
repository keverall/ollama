#============================================================================
# Title:            macos.conf
# Description:      macOS-specific service configuration (informational)
#                   macOS uses launchd, not systemd; this is a reference template
# Author:           Keverall
# Date:             2026-04-30
# Version:          1.0.0
#============================================================================

[Service]
# macOS doesn't use systemd; sod.sh manages Ollama directly on macOS
# This file serves as documentation for platform-specific env vars:
#
# Environment=OLLAMA_NUM_PARALLEL=24
# Environment=OLLAMA_MAX_LOADED_MODELS=2
# Environment=OLLAMA_FLASH_ATTENTION=1
# Environment=OLLAMA_KV_CACHE_TYPE=q4_0
